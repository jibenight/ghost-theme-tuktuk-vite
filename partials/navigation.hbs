{{#if isSecondary}}
  <ul class='flex flex-wrap justify-center text-center'>
    {{#foreach navigation}}
      <li class='mx-3 list-none'>
        <a
          href='{{url absolute='true'}}'
          class='inline-block text-base text-gray-900 hover:text-gray-700 dark:text-gray-200'
        >
          {{label}}
        </a>
      </li>
    {{/foreach}}
  </ul>
{{else}}
  <ul class='flex flex-col items-center justify-center'>
    <!-- Loop through the navigation items -->
    {{#foreach navigation}}
      <li class='mb-6'>
        <a
          href='{{url absolute='true'}}'
          class='inline-block text-lg xl:text-xl font-bold text-gray-900 dark:text-slate-200 hover:text-gray-700'
        >
          {{label}}
        </a>
      </li>
    {{/foreach}}
    <!-- End the loop -->
  </ul>
{{/if}}