{{!< default}}

{{#authors}}
<div class="container mx-auto {{#if cover_image}} bg-cover rounded-3xl bg-center" style="background-image: url('{{img_url cover_image size="xl"}}'); {{/if}}">
    <div class="2xl:flex mb-4 {{#if cover_image}} py-16 px-10{{/if}} overflow-hidden rounded-3xl"> 
        <div class="flex flex-wrap flex-col items-center {{#if cover_image}}lg:w-1/3{{else}}w-full{{/if}} p-5 bg-slate-100 dark:bg-slate-800 rounded-3xl">
            <div class="w-auto p-4">
                {{#if profile_image}}
                    <img class="rounded-full w-40" src="{{profile_image}}" alt="{{name}}">
                {{else}}
                    <img class="rounded-full w-40" src="/partials/icons/avatar.svg" alt="{{name}}">
                {{/if}}
            </div>
            <div class="flex-1 p-4">
                <div class="flex flex-col h-full">
                    <div class="flex-initial mb-4">
                        <h3 class="font-heading mb-2 text-4xl text-slate-800 dark:text-slate-200 font-black tracking-tight text-center">
                            {{name}}
                        </h3>
                        {{#if location}}
                            <p class="text-slate-400 dark:text-slate-200 flex justify-center items-center">
                                {{> "icons/map"}} {{location}}
                            </p>
                        {{/if}} 
                    </div>
                    <div class="flex-initial">
                        {{#if bio}}
                            <p class="mb-8 text-slate-900 dark:text-slate-200 font-bold text-center">{{bio}}</p>
                        {{/if}}
                        <div class="flex flex-wrap justify-center -m-1.5">
                            {{#if website }}
                            <div class="w-auto p-1.5">
                                <a class="flex items-center justify-center w-10 h-10 bg-slate-100 rounded-full" href="{{website}}">
                                    <img src="/partials/icons/site.svg" alt="">
                                </a>
                            </div>
                            {{/if}}
                            {{#if facebook }}
                            <div class="w-auto p-1.5">
                                <a class="flex items-center justify-center w-10 h-10 bg-slate-100 rounded-full" href="{{facebook_url}}">
                                    <img src="/partials/icons/facebook.svg" alt="">
                                </a>
                            </div>
                            {{/if}}
                            {{#if twitter }}
                            <div class="w-auto p-1.5">
                                <a class="flex items-center justify-center w-10 h-10 bg-slate-100 rounded-full" href="{{twitter_url}}">
                                    <img src="/partials/icons/twitter.svg" alt="">
                                </a>
                            </div>
                            {{/if}}
                        </div>
                    </div>
                </div>
            </div>
        </div> 
    </div>
</div>
{{/authors}}

<div class="container mx-auto my-5">
    <div class="py-16 px-2 lg:px-8 bg-slate-100 rounded-3xl dark:bg-slate-800">
        {{#authors}} 
            <div class="mb-10">
                <h2 class="italic dark:text-slate-200 text-center font-heading font-bold text-2xl sm:text-4xl">{{t "A collection of"}} {{plural ../pagination.total empty='zero posts' singular='% post' plural='% '}}{{t "posts"}}</h2>
            </div>
        {{/authors}}
        <div class="flex flex-wrap -m-4 mb-6 post-feed">
            {{#foreach posts}}
                {{> "post-card"}}
            {{/foreach}}
        </div>
        
    </div>
</div>
         
 
